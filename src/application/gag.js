import React from 'react';

const mystyle = {
  
  };


function Gag(props)
{
    return (
         <div>
             <h1>{props.title}</h1>
             <p>{props.text}</p>
             <img></img>
         </div>
      );
}

export default Gag;
